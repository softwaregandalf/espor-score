import CommunityThreadView from "@/components/CommunityThreadView";

export const metadata = {
  title: 'Konu Detayı | Nexus Pro',
};

export default function ThreadPage() {
  return <CommunityThreadView />;
}